import { TrainDirecDirective } from './train-direc.directive';

describe('TrainDirecDirective', () => {
  it('should create an instance', () => {
    const directive = new TrainDirecDirective();
    expect(directive).toBeTruthy();
  });
});
