import { TrainPipePipe } from './train-pipe.pipe';

describe('TrainPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrainPipePipe();
    expect(pipe).toBeTruthy();
  });
});
