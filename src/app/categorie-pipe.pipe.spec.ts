import { CategoriePipePipe } from './categorie-pipe.pipe';

describe('CategoriePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriePipePipe();
    expect(pipe).toBeTruthy();
  });
});
